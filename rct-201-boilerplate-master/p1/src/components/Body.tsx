import { Card } from "./Card";

function Body() {
  return (
    <div className="container">
      hey from body
      {/* Iterate over data and show `Card` here */}
    </div>
  );
}

export { Body };
